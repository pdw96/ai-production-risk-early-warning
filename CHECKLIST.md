# 머지 전 확인

**여기에는 상자가 없다.** `durable-writing` 이 영속 문서에 체크박스를 적지 않는다고
하고, 그 규칙에 「상태를 나타내는 것만」이라는 한정이 없다 — 같은 문서가
**「규칙을 쓴 문서가 그 규칙에서 면제되지 않는다」**고도 적는다.

**쓰는 법** — PR 마다 해당하는 절을 복사해 **거기서** 앞에 `- [ ]` 를 붙여 채운다.
저장소에는 평범한 목록으로 둔다. 여기서 채우면 다음 PR 에는 이미 거짓인 표시가
남기 때문이다.

**명령은 여기 적지 않는다.** `CLAUDE.md` 의 「명령」 절이 원본이고, 두 곳에 적으면
한쪽이 조용히 낡는다. 아래는 **무엇을 보고 통과시키는지**만 든다.

## 항상

- 백엔드 검사가 **두 엔진에서 각각** 통과한다 — 한쪽만 통과한 것은 통과가
  아니다. 로컬만 빨갛고 CI 가 초록이면 `.venv` 의 파이썬 판부터 의심한다.
- 프런트엔드 **검사 · 타입 검사 · 빌드**가 통과한다. 셋이다 — `npm test`(vitest)
  를 빠뜨리면 리스크 상태 변경 같은 상호작용 검사를 건너뛴 채 통과로 읽힌다
  (`npm run lint` 는 린터가 아니라 타입 검사다).
- **`Cloud validation` 이 이 head 에서 초록이다.** 로컬만으로는 안 되는 것이 거기
  들어 있다 — PostgreSQL 쪽 실행, 그리고 **FastAPI 와 Next.js 가 실제로 떠서 화면
  PNG 가 산출되는 것**이다. 위 두 줄을 다 채워도 이것을 안 보면 `PRD.md` 의 성공
  기준 중 기계가 세는 부분이 확인되지 않는다. **사람이 세는 부분은 초록으로 덮이지
  않는다** — 아래 「사용자 시나리오나 상태 변경 경로를 건드렸다면」이 그 자리다.
- 이 PR 이 **한 가지만** 한다.
- 새 의존성이 있다면 **왜 그것인지 세 줄**이 남아 있다.
- 주석과 커밋 메시지가 **무엇을 하는지가 아니라 왜 그런지**를 적는다.
- 지키지 않는 규칙을 상수나 단정으로 적지 않았다 — 적어 두면 지켜지는 것처럼
  보인다.
- **PR 이 문서에 적는 주장이 참이다.** 머지되지 않은 가지에서 잰 값을 사실로
  적지 않았고, 추측한 것은 추측이라고 적었다.

## 사용자 시나리오나 상태 변경 경로를 건드렸다면

**기계가 세지 않는 한 줄이 하나 있다.** `PRD.md` 의 성공 기준이 그것을 사람 몫으로
갈라 적어 두었는데, 목록에 없으면 `Cloud validation` 초록만 보고 넘어가게 된다.

- 화면에서 리스크 상태를 **실제로 눌러 바꾸고, 새로고침해도 남아 있는 것**을 봤다.
  백엔드 검사와 컴포넌트 검사는 화면이 `updateRiskStatus` 를 부르지 않게 되어도
  초록일 수 있다 — 그래서 이 한 줄은 사람이 본다.

## 「조용히 깨지는 자리」를 건드렸다면

무엇을 고치면 무엇이 깨지는지는 `CLAUDE.md` 의 그 표가 든다. 여기에 베껴 오지
않는다 — 표가 늘면 이쪽이 낡는다.

- 표에 든 파일을 건드렸으면 짝이 되는 검사를 **실제로 돌려** 확인했다.
- **표 밖의 짝은 기계가 안 보므로 직접 셌다** — 고친 사실을 요약하거나
  인용하던 자리를 전부 같이 고쳤다. 머지 하나가 문서 넷을 낡게 만드는 것이
  이 저장소의 상수다.

## 백엔드 검사를 더하거나 고쳤다면

**앵커볼트를 건드렸는지와 무관하다.** `CLAUDE.md` 의 엔진 규칙은 **엔진이 답을
바꿀 수 있는 검사 전부**와 **기준정보를 넣는 픽스처 전부**에 걸린다 — 서비스
회귀 검사 하나를 더하는 PR 도 여기 해당한다.

- 새 검사가 **스스로 엔진을 만들지 않고** 공용 픽스처를 받는다. 만들면 그
  검사는 한 엔진에서만 돌고 그 사실이 초록 뒤에 숨는다.
- 한쪽 엔진에서만 뜻이 서는 검사라면 건너뛴다고 적고, **그래서 무엇이
  검증되지 않는지**를 자기 자리에 적었다.
- 기준정보를 넣는 픽스처가 검사마다 표를 지웠다 다시 만든다 — 일회용
  데이터베이스는 모듈마다 하나여서 빠뜨리면 두 번째 검사부터 데이터가 두 벌이
  된다.

## 앵커볼트를 건드렸다면

**앵커볼트는 이 목록이다** — 데이터 모델과 스키마, ID 체계, 공개 API 계약.
`CLAUDE.md` 에는 이 정의가 없으므로 그쪽을 가리키지 않는다. 그 모양과 근거는
`docs/schema.md`, 그 위에서 도는 계산은 `docs/decision-rules.md` 가 든다. **인증 · 권한은 이
저장소에 아직 없고, 생기는 순간 여기 들어온다**(`PRD.md` 「하지 않을 일」).

- 스키마 변경에 **마이그레이션**이 있다.
- 리비전에 `downgrade` 가 비어 있지 않고, 되돌렸을 때 무엇이 사라지는지 적혀
  있다.
- **되돌리기를 실제로 돌려 봤다 — 일회용 데이터베이스에서.** 적어 둔 `downgrade`
  와 도는 `downgrade` 는 다른 것이다. **설정이 가리키는 곳에 그냥 돌리지
  않는다** — 거기서 돌린 것은 확인이 아니라 사고다. 돌릴 길이 없으면 **이 PR 이
  그 길을 세우거나, 못 돌렸다는 것과 그래서 무엇이 검증되지 않았는지를 PR 에
  적는다.** 「길이 없으니 건너뛴다」가 조용히 기본값이 되면, 깨진 롤백이 본문만
  채운 채 머지된다.
- 시드나 판정 규칙이 바뀌었으면 골든 케이스의 값을 다시 박았다.
- 단위·로트·유효기간 규칙이 깨지지 않았다. 무엇이 규칙인지는 `docs/schema.md`
  가 들고, 여기 요약하지 않는다 — 요약은 원본이 바뀌는 날 조용히 거짓이 된다.

## 배포를 더하는 PR 이라면

**밖에서 닿는 주소를 만드는 PR 이 이 절에 걸린다.** 「지금은 해당 없음」이라고
적어 두지 않는다 — 저장소가 지금 배포되는지는 **상태**여서 이 파일이 아니라 이슈와
PR 이 들고(`durable-writing`), 무엇보다 **처음으로 배포를 더하는 PR 이 정확히 여기
걸리는 PR** 이기 때문이다. 그 PR 의 저자에게 「해당 없음」이라고 말하면 아래 셋을
건너뛰라고 말하는 셈이 된다.

**어느 배포든 `PRD.md` 의 「제약 — 호스팅」을 함께 고친다** — 거기 적힌 세 곳
(로컬 · Docker Compose · GitHub Actions) 밖으로 나가는 일이기 때문이다. **공개
인터넷 금지 줄까지 고치는 것은 실제로 공개로 올릴 때뿐이다** — 사내망이나 VPN
안쪽에만 올리는 PR 은 그 줄을 건드리지 않는다. 둘을 묶어 두면 비공개 배포에
공개 노출을 끼워 넣거나, 사실과 다른 PRD 를 쓰게 된다. 어느 쪽이든 범위의
상한선을 옮기는 일이라 저자가 정한다.

- 프로덕션 에러의 원인을 10분 안에 찾아갈 경로가 있다.
- 롤백 절차를 **실제로 한 번 해 봤다.** 적어 둔 절차와 해 본 절차는 다른 것이다.
- **그 전에 인증 · 권한이 선다.** 밖에서 닿는 주소가 생기는 것과 로그인이 없는
  것은 같이 둘 수 없다 — `PRD.md` 가 그 둘을 한 짝으로 묶어 둔 이유다.
