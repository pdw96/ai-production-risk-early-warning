# AI 생산 리스크 조기경보 MVP — 범위와 제약

이 파일은 **범위와 제약**만 적는다. 무엇을 만드는지 — 문제 정의 · 데이터 모델 ·
판정 규칙 — 는 `README.md` 에 있고 **여기로 옮겨 적지 않는다.** 두 곳에 적으면
한쪽이 조용히 낡고, 산문에는 그 짝을 보는 기계가 없기 때문이다(`test_readme.py`
가 보는 것은 `README.md` 쪽이다).

## 핵심 사용자와 시나리오

- 사용자: 소재 제조공장의 **생산관리자 한 명.**
- 시나리오: 운영 현황 화면을 연다 → 납기가 위험한 오더와 14일 안에 부족해지는
  자재를 **왜 위험한지의 숫자와 함께** 본다 → 그중 하나를 골라 리스크 상태를
  `신규` → `확인 중` → `조치 완료` 로 옮겨 적는다.

앱의 **쓰기 경로는 이 상태 변경 하나뿐이다.** 그 사실이 아래 「하지 않을 일」의
첫 항목이 서는 근거다 — 읽기만 하는 화면에 권한 모형을 통째로 지을 이유가 없다.
쓰기가 하나 더 생기는 날 그 근거가 약해지므로, 그때 다시 본다.

## 성공 기준

**사람이 할 수 있는 일** — 위 시나리오가 화면에서 끊기지 않고 끝까지 간다. 위험
하나마다 근거가 같이 보인다: 납기는 잔여 수량과 최근 7일 평균 생산량에서 나온
완료예정일, 자재는 **재고가 0 이하로 가는 경우에는 소진일**, 원인이 유효기간
폐기면 그 사실이다.

**자재 경고 전부에 소진일이 붙지는 않는다.** 14일 안에 안전재고 미만으로만 내려가는
경우는 `stockout_date` 가 비어 있다. **다만 날짜가 아예 없다는 뜻은 아니다** — 그중
부족의 원인이 유효기간 폐기인 갈래는 근거 문장이 **부족이 시작되는 날을 함께 적는다**
(`first_shortage_date` 가 그 값이고, 응답에 별도 칸으로는 싣지 않는다). **그러므로
「모든 자재 위험에 소진일」은 기준이 아니다.** 그 칸을 내보내는 것은 기준을 넓히는
결정이라, 넓히는 PR 이 이 문단을 함께 고친다.

**기계가 세는 것** — `Cloud validation` 워크플로가 초록이다. 그 안에 든 것:
백엔드 검사가 **두 엔진에서 각각** 통과, 골든 케이스가 시드 전개를 통째로 통과,
프런트엔드 검사·타입 검사·빌드 통과, FastAPI 와 Next.js 가 실제로 떠서 화면 PNG 가
산출됨.

**다만 위 시나리오를 CI 가 판정하지는 않는다.** 기동 확인은 화면과 API 가 떴는지만
보고 그 뒤는 스크린샷이라, **앱의 하나뿐인 쓰기 경로(상태를 바꾸는 `PATCH`)를
어디서도 부르지 않는다** — 눌러 저장되는 것까지 훑는 단계가 없다. 화면이
`updateRiskStatus` 를 부르지 않게 되어도 백엔드 검사와 컴포넌트 검사는 초록일 수
있다. **그 한 줄은 사람이 확인한다** — `CHECKLIST.md` 의 「사용자 시나리오나 상태
변경 경로를 건드렸다면」이 그 자리다. 기계가 그것을 판정하게 되는 PR 이 이 문단을
함께 고친다.

「돌아가는 것 같다」는 기준이 아니다. 다만 **무엇을 기계가 세고 무엇을 사람이 세는지
갈라 적지 않으면 그 문장이 거짓이 된다.**

## 하지 않을 일

- **인증 · 권한 · 다중 사용자를 넣지 않는다.** 로그인 없는 단일 사용자 로컬
  데모로 둔다. 인증은 앵커볼트 층이라 나중에 넣으면 받는 자리가 많지만, 지금 넣는
  값은 그보다 작다 — 쓰기 경로가 하나뿐이기 때문이다. 넣기로 바뀌는 날은 이 줄을
  고치는 것이 아니라 **조각을 새로 연다.**
- **공개 인터넷에 올리지 않는다.** 금지하는 것은 **공개 인터넷에서 닿는 주소**다.
  사내망이나 VPN 안쪽의 주소는 이 줄이 아니라 아래 「제약 — 호스팅」이 막는다 —
  거기가 **도달 범위**로 금을 긋는다. 한 가지를 두 줄이 함께 금지하면 배포가 하나
  느는 날 어느 줄을 고칠지가 갈리고, **어디서 도는지를 여기 베껴 오면** 한쪽만
  고쳐진다.

  **인증을 미룬 근거는 이 줄이 아니라 「밖에서 닿는 주소가 없다」는 것이다** —
  공개든 아니든 그런 주소가 생기는 날 인증이 함께 서야 한다. 그 자리는
  `CHECKLIST.md` 「배포를 더하는 PR 이라면」이고, **둘은 거기서 같이 움직인다.**

  **그것이 규율에 그치는지 사실인지는 둘이 함께 정한다 — 포트 바인딩과 Docker
  Engine 판이다.** `compose.yaml` 이 게시 포트에 호스트 IP 를 적지 않으면 Docker 는
  **모든 인터페이스**에 묶고, 두 컨테이너는 `0.0.0.0` 에서 들으므로 LAN 이나 공개
  호스트에서 띄우는 순간 **인증 없는 API 가 밖에서 닿는다** — 상태를 바꾸는 `PATCH`
  까지. `127.0.0.1` 이 박혀 있고 **그와 함께** Engine 이 충분히 새것이면 같은 문장이
  **기술적 불변식**이 된다 — 옛 Engine 에서는 로컬호스트로 게시한 포트도 같은
  네트워크의 다른 호스트에서 닿을 수 있다고 알려져 있어 **포트만으로는 서지 않는다.**

  **어느 쪽인지는 여기서 세지 않는다** — 상태를 두 곳에 적으면 한쪽이 조용히 낡기
  때문이다. 바인딩은 `compose.yaml` 이 들고, **최소 Engine 판은 `README.md` 의 사전
  요구사항이 들어야 한다** — 거기에 판이 적혀 있지 않으면 그 조건은 아직 서지 않은
  것이다. **둘 중 하나라도 안 서면 이 항목은 여전히 규율이다.**
- **실제 회사 · 제품 · 거래처 · 운영 수치 · 개인 경험을 쓰지 않는다.** 데이터는
  전부 재현 가능한 합성 샘플이고, 그래서 이 저장소의 출력은 운영 의사결정이나
  성능 · 정확도 검증에 쓸 수 없다(`README.md` 「합성 데이터 기반 데모의 한계」).

**「나중에 할 일」은 하지 않을 일이 아니다.** 미룬 것이 무엇이고 어느 것이 먼저인지는
`README.md` 「향후 확장 계획」이 들고, 그쪽이 원본이다. **여기에 목록을 옮겨 적지
않는다** — 베껴 오면 그쪽에서 하나가 구현되어 빠지는 날 이 문단만 낡는다.

## 제약

- **스택** — 판 번호는 여기 적지 않는다. **가리키기만 하는 이유가 있다** —
  `backend/tests/test_claude_md.py` 는 워크플로와 `backend/Dockerfile` 에서 파이썬
  판을 읽어 서로 맞는지 보고, 이어 `CLAUDE.md` 와 `README.md` 가 그 판을 적는지
  본다(**문서마다 한 번만** 적는지는 뒤의 두 문서에만 건다). **이 파일은 그 넷에
  없다.** 여기 숫자를 적으면 판이 오를 때 넷은 검사에 밀려 고쳐지고 이 줄만 낡은
  채로 남는다. 다만 **의존성 판과 런타임 판은 사는 곳이 다르다** — 의존성은
  `backend/requirements.txt` 와 `frontend/package.json` 이 들고, 런타임은 파이썬이
  `CLAUDE.md` 의 파이썬 표제, 노드가 `README.md` 의 사전 요구사항(최소 판)과
  `frontend/Dockerfile` · CI 워크플로(**실제로 도는 판**)다. **`frontend/package.json` 에는 `engines` 가 없다**(실측 2026-09-15) —
  없는 것을 가리키면 고치러 간 사람이 빈손으로 돌아온다.
- **호스팅** — **띄운 사람 말고는 닿을 수 없는 자리에서만 돈다.** 개발자 자기
  기계와 CI 잡 안이 그것이다. **남이 닿는 주소에 두지 않는다 — 공개든 사내망이든
  VPN 안쪽이든 같다.**

  **실행 방식으로는 이 금을 그을 수 없다.** `compose.yaml` 은 머리에 「일반 Docker
  호스트(로컬 PC, **서버**)용 구성」이라고 적혀 있어, 「Compose 로 돈다」를 허용으로
  적으면 **사내망 서버가 그대로 통과한다.** 그래서 어떻게 도는가가 아니라 **어디서
  닿는가**로 적는다.

  **이 줄은 지금 무엇이 보장되는지를 말하지 않는다** — **하지 않기로 한 것**을
  말한다. 닿는 범위가 실제로 묶이는지는 위 「하지 않을 일」의 포트 바인딩 문단이
  조건으로 든다.
- **외부 API — 없다.** 셀 쿼터의 대상이 없다. 의존성 어디에도 외부 서비스 호출이
  없는 것이 이 값을 지키는 방법이고, 하나 들이는 순간 이 줄이 거짓이 된다.
- **월 비용 한도 — 0원.** 무료 한도를 넘기면 돈을 더 내는 것이 아니라 **쓰는
  방식을 바꾼다.**

저장소는 공개다 — 인증 없는 GitHub API 조회가 `200` 을 돌려주었다(**2026-09-14
실측**). 공개 저장소의 표준 러너에는 분 단위 과금이 없으므로 위 한도와 어긋나지
않는다. **이것은 규칙이 아니라 측정이다** — 요금제나 공개 여부가 바뀌면 다시 잰다.
지금 남은 한도는 이 문서가 아니라 저장소의 Settings → Billing 이 든다.
