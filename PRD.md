# AI 생산 리스크 조기경보 MVP — 범위와 제약

이 파일은 **무엇을 왜 만드는가, 그리고 어디까지인가**를 든다. 나머지는 목적이
달라 따로 산다 — 두 곳에 적으면 한쪽이 조용히 낡고, 산문에는 그 짝을 보는 기계가
없기 때문이다.

| 묻는 것 | 사는 곳 |
| --- | --- |
| 무엇을 왜, 어디까지 만드는가 | **이 파일** |
| 엔티티와 관계는 어떻게 생겼나 | `docs/schema.md` |
| 무엇을 보고 위험이라 하는가 | `docs/decision-rules.md` |
| 어떻게 돌리고 무엇이 보이나 | `README.md` |
| 이 저장소에서 어떻게 일하는가 | `CLAUDE.md` |
| 머지 전에 무엇을 보는가 | `CHECKLIST.md` |

## 문제

생산 계획과 실적, 재고와 예정 입고가 흩어져 있으면 납기 지연과 자재 부족을 조기에 발견하기 어렵습니다. 이 MVP는 결정적인 계산 규칙으로 위험 근거를 표시하고, 담당자가 리스크 상태를 `신규` → `확인 중` → `조치 완료`로 기록할 수 있게 합니다.

## 설계 착안점

실제 생산관리 화면에서 유용한 밀도 높은 통제실형 정보 구조를 참고해 KPI, 납기 오더, 14일 자재 전망, 권장 조치를 연결했습니다. 특정 기업의 화면이나 실제 업무 프로세스를 복제한 것이 아니라, 운영 의사결정에 필요한 정보 흐름을 합성 데이터로 표현한 설계입니다.

## 핵심 사용자와 시나리오

- 사용자: 소재 제조공장의 **생산관리자 한 명.**
- 시나리오: 운영 현황 화면을 연다 → 납기가 위험한 오더와 14일 안에 부족해지는
  자재를 **왜 위험한지의 숫자와 함께** 본다 → 그중 하나를 골라 리스크 상태를
  `신규` → `확인 중` → `조치 완료` 로 옮겨 적는다.

앱의 **쓰기 경로는 이 상태 변경 하나뿐이다.** 그 사실이 아래 「하지 않을 일」의
첫 항목이 서는 근거다 — 읽기만 하는 화면에 권한 모형을 통째로 지을 이유가 없다.
쓰기가 하나 더 생기는 날 그 근거가 약해지므로, 그때 다시 본다.

## 성공 기준

**사람이 할 수 있는 일** — 위 시나리오가 화면에서 끊기지 않고 끝까지 간다. 위험
하나마다 근거 숫자가 같이 보인다: 납기는 잔여 수량과 최근 7일 평균 생산량에서 나온
완료예정일, 자재는 부족이 처음 나는 소진일과 권장 조치이며, 원인이 유효기간 폐기면
근거가 그 사실을 가리킨다. 규칙 자체는 `docs/decision-rules.md` 가 든다.

**기계가 세는 것** — `Cloud validation` 워크플로가 초록이다. 그 안에 든 것:
백엔드 검사가 **두 엔진에서 각각** 통과, 골든 케이스가 시드 전개를 통째로 통과,
타입 검사와 빌드 통과, FastAPI 와 Next.js 가 실제로 떠서 화면 PNG 가 산출됨.

「돌아가는 것 같다」는 기준이 아니다. 판정은 사람이 아니라 검사가 한다.

## 만드는 순서 — 0 · 1 · 2단계

이 저장소는 세 단계로 나뉩니다.

**0단계 — 환경 조성.** 규칙·검사·자동화를 세워, 세션이 **관제탑 없이도 같은 규칙으로 굴러가게** 합니다. 마일스톤 `P0`·`P1`·`P2` 가 **전부 이 단계 안**에 삽니다.

**1단계 — ERP Setup.** 기준정보·구매·재고·생산·영업·품질이 한 벌로 도는 ERP 를 세웁니다. **시작점은 「조기경보 ERP 설계도」** 입니다 — 무엇을 세울지는 거기서 정합니다. 저장소 밖 아티팩트이고 주소는 `CLAUDE.md` 의 아티팩트 목록에 한 번만 적습니다(두 곳에 적으면 한쪽이 조용히 낡습니다). [화면 안내](README.md#화면-안내)는 지금 **서 있는 것만** 보여 줍니다.

<sub>**1단계를 여는 첫 일은 그 설계도를 [프로젝트](../../projects)·[마일스톤](../../milestones)·[이슈](../../issues)로 쪼개는 것**입니다 — 쪼개기 전에는 1단계가 시작되지 않습니다. 아티팩트 안의 그림은 진척을 세지 못하기 때문입니다.</sub>

<sub>**[`docs/2026-09-07-stage-1-report.md`](docs/2026-09-07-stage-1-report.md) 의 「1단계」·「2단계」는 이 눈금이 아닙니다.** 그 문서 첫 줄이 적듯 **「설계도 41판이 남긴 인수인계」의 눈금** — 설계도 **안쪽**을 세는 번호입니다. 날짜가 붙은 `docs/` 문서가 다 그런 것은 아닙니다 — 예를 들어 [`docs/2026-09-11-env-stopline.md`](docs/2026-09-11-env-stopline.md) 는 여기 눈금을 그대로 씁니다. **어느 눈금인지는 그 문서가 적습니다.**</sub>

**2단계 — ERP 기반 리스크 조기 탐지 Setup.** 그 ERP 의 값에서 납기·자재 위험을 계산해 조기에 띄웁니다. [판정 규칙](docs/decision-rules.md)이 그 자리입니다.

**어디까지 왔는지는 이 파일이 들지 않습니다** — 손으로 적은 진척은 반드시 낡습니다. [열린 이슈](../../issues)와 [마일스톤](../../milestones)이 셉니다.

**마일스톤이 세는 것은 0단계입니다** — `P0`·`P1`·`P2` 가 그것입니다. **1단계의 자리는 위의 쪼개기가 만듭니다.** 어느 단계가 어디까지 찼는지는 이 파일이 아니라 그 링크들이 답합니다.

## 하지 않을 일

- **인증 · 권한 · 다중 사용자를 넣지 않는다.** 로그인 없는 단일 사용자 로컬
  데모로 둔다. 인증은 앵커볼트 층이라 나중에 넣으면 받는 자리가 많지만, 지금 넣는
  값은 그보다 작다 — 쓰기 경로가 하나뿐이기 때문이다. 넣기로 바뀌는 날은 이 줄을
  고치는 것이 아니라 **조각을 새로 연다.**
- **공개 인터넷에 올리지 않는다.** 로컬 실행 · `compose.yaml` · CI 안에서만
  돈다. **밖에서 닿는 주소가 없다는 것이 위 항목의 전제이므로 둘은 같이 움직인다** —
  한쪽만 바꾸지 않는다.
- **실제 회사 · 제품 · 거래처 · 운영 수치 · 개인 경험을 쓰지 않는다.** 데이터는
  전부 재현 가능한 합성 샘플이다 — 그 대가를 아래 「한계」가 적는다.

## 합성 데이터 기반 데모의 한계

데이터와 위험 패턴은 재현 가능한 합성 샘플이며 실제 수요, 공급업체, 생산능력, 품질 또는 설비 상태를 의미하지 않습니다. 따라서 운영 의사결정이나 성능·정확도 검증에 직접 사용할 수 없습니다. 외부 LLM 연동도 포함하지 않습니다.

## 나중에 할 일

**이것은 「하지 않을 일」이 아니다.** 위의 셋은 범위의 상한선이고, 아래는 순서다 —
지금 안 할 뿐 하기로 되어 있다. 둘을 같은 목록에 두면 상한선이 흐려진다.

**LOT/FIFO**는 [14일 안전재고 — LOT/FIFO 기반](docs/decision-rules.md#14일-안전재고--lotfifo-기반)에, **완제품 로트·제품창고**와 **품질관리(IQC·PQC·OQC)** 는 [완제품 로트와 출하검사](docs/decision-rules.md#완제품-로트와-출하검사)에 구현되어 있습니다. 남은 계획을 우선순위 순으로 적습니다.

1. **출하 리스크**: 출하 일정과 완제품 재고를 비교해 부족을 경보합니다. 납기·자재에 이은 세 번째 리스크 타입이며, 기존 납기 리스크와 같은 사건을 두 번 경보할 수 있어 통합 설계가 선행돼야 합니다. 차감은 제품창고의 OQC 합격 로트에서만 일어납니다.
2. **재고이동 처리**: 창고 간 재고이동을 행위로 기록하고 이력을 남깁니다. 현재 앱의 쓰기 경로는 리스크 상태 변경 하나뿐이라, 이는 기능 추가가 아니라 쓰기 워크플로 도입입니다. 자재의 `원재료창고 → 생산창고`가 그 대상입니다. 완제품의 `생산창고 → 제품창고`는 출하검사 합격이 결정하므로 별도의 이동 행위가 아닙니다.
3. **검사 결과를 판정에 반영**: IQC 불합격 자재의 반품·격리를 자재 가용 재고에 반영하고, 검사 기준·항목·측정값을 관리합니다. 지금 검사 기록은 판정 결과만 담고, 재고 흐름을 바꾸는 것은 OQC뿐입니다.
4. **설비**: 가동률·고장·정비 일정을 연결해 생산능력 리스크를 추가합니다.
5. **AI 브리핑**: 검증 가능한 계산 근거를 입력으로 삼아 담당자용 일일 브리핑을 생성하되, 실제 모델 연동과 권한·감사 로그를 별도로 설계합니다.

## 제약

- **스택** — 판 번호는 여기 적지 않는다. `CLAUDE.md` 의 표제(「파이썬은 3.12 다」)
  와 `backend/requirements.txt` · `frontend/package.json` 이 든다.
- **호스팅** — 로컬 · Docker Compose · GitHub Actions. 그 밖은 없다.
- **외부 API — 없다.** 셀 쿼터의 대상이 없다. 의존성 어디에도 외부 서비스 호출이
  없는 것이 이 값을 지키는 방법이고, 하나 들이는 순간 이 줄이 거짓이 된다.
- **월 비용 한도 — 0원.** 무료 한도를 넘기면 돈을 더 내는 것이 아니라 **쓰는
  방식을 바꾼다.**

저장소는 공개다 — 인증 없는 GitHub API 조회가 `200` 을 돌려주었다(**2026-09-14
실측**). 공개 저장소의 표준 러너에는 분 단위 과금이 없으므로 위 한도와 어긋나지
않는다. **이것은 규칙이 아니라 측정이다** — 요금제나 공개 여부가 바뀌면 다시 잰다.
지금 남은 한도는 이 문서가 아니라 저장소의 Settings → Billing 이 든다.
